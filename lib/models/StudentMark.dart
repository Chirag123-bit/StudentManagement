class StudentMark {
  String? name, batch;
  double? api, iot, web, total;
  double? percentage;
  StudentMark(this.name, this.batch, this.api, this.iot, this.web, this.total,
      this.percentage);
}
